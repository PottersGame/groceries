{
  "name": "pantrypal-sk-ingestion",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — CRON-triggered flyer PDF ingestion via Gemini vision AI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute pantrypal-sk-db --file=schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.114.17"
  }
}
